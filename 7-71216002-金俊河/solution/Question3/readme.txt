测试计划:
通过for文输入a,b 第一个输入到的a,b是计算Rectangle的，a和b就是计算Rectangle面积的相关参数。输入的a和b是Rectangle.getFactory()方法会将Shape对象进行初始化后输出这个Rectangle的面积（方法回调）。
第二次输入到的是计算Rhombus的面积的相关参数。第三次输入到的是计算Ellipse的面积的相关参数。同理进行输入能得所有形状的面积。
